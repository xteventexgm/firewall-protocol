/**
 * Autenticación JWT — reservado para fase futura.
 *
 * Cuando se implemente:
 * - Validar token en conexión socket o middleware HTTP.
 * - Usar `JWT_SECRET` desde `.env` / `config/env.ts`.
 *
 * @see backend-server/README.md — sección "Pendiente"
 */
export const JWT_PENDING = true;
